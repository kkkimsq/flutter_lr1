name: lr1
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.1.2  # ← Добавьте эту строку
  http: ^1.1.0              # Дополнительная зависимость

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0